# Probe library

<!--
Team-local accreted probe patterns (cadence-spec.md §9.4). Starts empty on
purpose.

When an interview catches something real — a scope activation nobody saw, an
assumption that later broke — append the PATTERN here, one line, in the form
`trigger → check`:

    touches cancellation → state-rule check
    touches PII → privacy review
    first un-flagged launch → a11y audit

The interview loads this file before every Full-tier interview and asks the
matching checks. Over time this becomes org-specific — and portable — IP at
the level of *question patterns*, not answers, so this file contains no
project details, no names, no dates: just triggers and checks.
-->
